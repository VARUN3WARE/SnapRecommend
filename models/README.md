Models
------
- Contains model scaffolds: two-tower retrieval, ranker, user encoder, and CLIP encoder wrappers.
- Checkpoints: saved under paths configured in `config.py` (e.g., `RANKER_CHECKPOINT_PATH`).
- Training: see `pipeline/` scripts for training orchestration and checkpointing.
